PyYAML==6.0.1
pyroute2==0.7.2        # Debian 12 bookworm full package доступен
python-json-logger==2.0.7
# Для работы с nftables через libnftables JSON API:
# (в образе manager ставим apt-пакет python3-nftables)
